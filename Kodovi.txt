CREATE TABLE OSOBA(
    ID INT GENERATED ALWAYS AS IDENTITY,
    IME VARCHAR2(25) NOT NULL,
    PREZIME VARCHAR2(30) NOT NULL,
    JMBG CHAR(13) NOT NULL,
    ADRESA VARCHAR2(100),
    POL CHAR(1 CHAR) NOT NULL,
    DATUM_RODJENJA DATE NOT NULL,
    EMAIL VARCHAR2(100) NOT NULL,
    KOMENTAR VARCHAR2(100),
    
    CONSTRAINT PK_OSOBA PRIMARY KEY (ID),
    CONSTRAINT UQ_OSOBA_JMBG UNIQUE (JMBG),
    CONSTRAINT UQ_OSOBA_EMAIL UNIQUE (EMAIL),
    CONSTRAINT CK_OSOBA_JMBG CHECK (REGEXP_LIKE(JMBG, '^[0-9]{13}$')),
    CONSTRAINT CK_OSOBA_POL CHECK (POL IN ('M', 'Z', 'Ž')),
    CONSTRAINT CK_OSOBA_EMAIL CHECK (REGEXP_LIKE(EMAIL, '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$'))
);
	
CREATE TABLE RODITELJ_STARATELJ(
    ID INT GENERATED ALWAYS AS IDENTITY,
    ID_OSOBA INT NOT NULL,
    ZANIMANJE VARCHAR2(50) NOT NULL,
    RADNO_MESTO VARCHAR2(50),
    
    CONSTRAINT PK_RODITELJ PRIMARY KEY (ID),
    CONSTRAINT FK_RODITELJ_OSOBA FOREIGN KEY (ID_OSOBA) REFERENCES OSOBA(ID)
);

CREATE TABLE NASTAVNIK(
    ID INT GENERATED ALWAYS AS IDENTITY,
    ID_OSOBA INT NOT NULL,
    STRUCNA_SPREMA VARCHAR2(20) NOT NULL,
    STATUS VARCHAR2(20) NOT NULL,
    ZVANJE VARCHAR2(50) NOT NULL,
    DATUM_ZAPOSLENJA DATE NOT NULL,
    
    CONSTRAINT PK_NASTAVNIK PRIMARY KEY (ID),
    CONSTRAINT FK_NASTAVNIK_OSOBA FOREIGN KEY (ID_OSOBA) REFERENCES OSOBA(ID),
    CONSTRAINT CK_NASTAVNIK_STATUS CHECK (STATUS IN ('AKTIVAN', 'ODSUTAN', 'NA BOLOVANJU', 'PENZIONISAN'))
);

CREATE TABLE ODELJENJE(
    ID INT GENERATED ALWAYS AS IDENTITY,
    OZNAKA VARCHAR2(10) NOT NULL,
    SKOLSKA_GODINA VARCHAR2(10) NOT NULL,
    RAZRED NUMBER NOT NULL,
    
    CONSTRAINT PK_ODELJENJE PRIMARY KEY (ID),
    CONSTRAINT UQ_ODELJENJE UNIQUE (RAZRED, OZNAKA, SKOLSKA_GODINA),
    CONSTRAINT CK_ODELJENJE_RAZRED CHECK (RAZRED BETWEEN 1 AND 8),
    CONSTRAINT CK_ODELJENJE_GODINA CHECK (REGEXP_LIKE(SKOLSKA_GODINA, '^[1-9][0-9]{3}/[1-9][0-9]{3}$'))
);

CREATE TABLE UCENIK(
    ID INT GENERATED ALWAYS AS IDENTITY,
    ID_OSOBA INT NOT NULL,
    STATUS VARCHAR2(10) NOT NULL,
    GODINA_UPISA VARCHAR2(10) NOT NULL,
    
    CONSTRAINT PK_UCENIK PRIMARY KEY (ID),
    CONSTRAINT FK_UCENIK_OSOBA FOREIGN KEY (ID_OSOBA) REFERENCES OSOBA(ID),
    CONSTRAINT CK_UCENIK_GODINA_UPISA CHECK (REGEXP_LIKE(GODINA_UPISA, '^[1-9][0-9]{3}/[1-9][0-9]{3}$'))
);

CREATE TABLE STARATELJSTVO(
    ID INT GENERATED ALWAYS AS IDENTITY,
    ID_UCENIK INT NOT NULL,
    ID_STARATELJ INT NOT NULL,
    
    CONSTRAINT PK_STARATELJSTVO PRIMARY KEY(ID),
    CONSTRAINT FK_STARATELJSTVO_UCENIK FOREIGN KEY(ID_UCENIK) REFERENCES UCENIK(ID),
    CONSTRAINT FK_STARATELJSTVO_STARATELJ FOREIGN KEY(ID_STARATELJ) REFERENCES RODITELJ_STARATELJ(ID),
    CONSTRAINT UQ_STARATELJ_UCENIIK UNIQUE(ID_UCENIK, ID_STARATELJ)
);

CREATE TABLE UCENIK_ODELJENJE(
    ID INT GENERATED ALWAYS AS IDENTITY,
    ID_UCENIK INT NOT NULL,
    ID_ODELJENJE INT NOT NULL,
    
    CONSTRAINT PK_UCENIK_ODELJENJE PRIMARY KEY(ID),
    CONSTRAINT FK_ODELJENJE_UCENIK FOREIGN KEY(ID_UCENIK) REFERENCES UCENIK(ID),
    CONSTRAINT FK_ODELJENJE_ODELJENJE FOREIGN KEY(ID_UCENIK) REFERENCES ODELJENJE(ID),
    CONSTRAINT UQ_UCENIK_ODELJENJE UNIQUE(ID_UCENIK, ID_ODELJENJE)
);

CREATE TABLE PREDMET(
    ID INT GENERATED ALWAYS AS IDENTITY,
    NAZIV VARCHAR2(50) NOT NULL,
    SKOLSKA_GODINA VARCHAR2(10) NOT NULL,
    RAZRED NUMBER(1) NOT NULL,
    NEDELJNI_FOND NUMBER NOT NULL,
    TIP VARCHAR2(12) NOT NULL,
    OPIS VARCHAR2(50),
    KOMENTAR VARCHAR2(50)
    
    CONSTRAINT PK_PREDMET PRIMARY KEY(ID),
    CONSTRAINT CK_RAZRED_PREDMET CHECK(RAZRED BETWEEN 1 AND 8),
    CONSTRAINT CK_TIP_PREDMETA CHECK(TIP IN ('OBAVENI', 'IZBORNI', 'FAKULTATIVNI')),
    CONSTRAINT CK_PREDMET_GODINA CHECK (REGEXP_LIKE(SKOLSKA_GODINA, '^[1-9][0-9]{3}/[1-9][0-9]{3}$')) 
);

CREATE TABLE SLUSA_PREDMET(
    ID INT GENERATED ALWAYS AS IDENTITY,
    ID_UCENIK INT NOT NULL,
    ID_PREDMET INT NOT NULL,
    
    CONSTRAINT PK_UCENIK_SLUSA PRIMARY KEY(ID),
    CONSTRAINT FK_PREDMET_UCENIK FOREIGN KEY(ID_UCENIK) REFERENCES UCENIK(ID),
    CONSTRAINT FK_UCENIK_PREDMET FOREIGN KEY(ID_PREDMET) REFERENCES PREDMET(ID),
    CONSTRAINT UQ_UCENIK_ODELJENJE UNIQUE(ID_UCENIK, ID_PREDMET)
);


CREATE TABLE NASTAVA(
    ID INT GENERATED ALWAYS AS IDENTITY,
    ID_PREDMET INT NOT NULL,
    ID_NASTAVNIK INT NOT NULL,
    ID_ODELJENJE INT NOT NULL,
    
    CONSTRAINT PK_NASTAVA PRIMARY KEY(ID),
    CONSTRAINT FK_NASTAVA_PREDMET FOREIGN KEY(ID_PREDMET) REFERENCES PREDMET(ID),
    CONSTRAINT FK_NASTAVA_NASTAVNIK FOREIGN KEY(ID_NASTAVNIK) REFERENCES NASTAVNIK(ID),
    CONSTRAINT FK_NASTAVA_ODELJENJE FOREIGN KEY(ID_ODELJENJE) REFERENCES ODELJENJE(ID),
    CONSTRAINT UQ_NASTAVA UNIQUE(ID_PREDMET, ID_NASTAVNIK, ID_ODELJENJE)
);

CREATE TABLE IZOSTANAK(
    ID_UCENIK INT NOT NULL,
    DATUM DATE NOT NULL,
    REDNI_BROJ_CASA INT NOT NULL,
    TIP VARCHAR2(10) NOT NULL,
    ID_NASTAVA INT NOT NULL, 
    RAZLOG_IZOSTANKA VARCHAR2(50),
    OPRAVDAO VARCHAR2(10),
    KOMENTAR VARCHAR(50),
    
    CONSTRAINT PK_IZOSTANAK PRIMARY KEY(ID_UCENIK, DATUM, REDNI_BROJ_CASA),
    CONSTRAINT FK_NASTAVA FOREIGN KEY(ID_NASTAVA) REFERENCES NASTAVA(ID),
    CONSTRAINT CK_LOGIKA_OPRAVDANJA CHECK(
        TIP = 'OPRAVDAN' AND OPRAVDAO IN ('STARATELJ', 'LEKAR')
        OR
        TIP = 'NEOPRAVDAN' AND OPRAVDAO IS NULL
    )
);

CREATE TABLE OCENA(
    ID INT GENERATED ALWAYS AS IDENTITY,
    ID_UCENIK NOT NULL,
    ID_NASTAVA NOT NULL,
    TIP VARCHAR2(20) NOT NULL,
    VREDNOST INT NOT NULL,
    POLUGODJE INT NOT NULL,
    DATUM_OCENJIVANJA DATE NOT NULL,
    KOMENTAR VARCHAR2(50),
    
    CONSTRAINT PK_OCENA PRIMARY KEY(ID),
    CONSTRAINT FK_OCENA_UCENIK FOREIGN KEY(ID_UCENIK) REFERENCES UCENIK(ID),
    CONSTRAINT FK_OCENA_NASTAVA FOREIGN KEY(ID_NASTAVA) REFERENCES NASTAVA(ID),
    CONSTRAINT CK_TIP_OCENE CHECK(TIP IN ('PISANA PROVERA', 'USMENI ODGOVOR', 'AKTIVNOST', 'ZAKLJUCNA')),
    CONSTRAINT CK_POLUGODJE CHECK(POLUGODJE IN (1,2)),
    CONSTRAINT CK_VREDNOST_OCENE CHECK(VREDNOST BETWEEN 1 AND 5)
);

CREATE TABLE RAZREDNI_STARESINA(
    ID_NASTAVNIK INT NOT NULL, 
    ID_ODELJENJE INT NOT NULL,
    BROJ_ODRZANIH_SASTANAKA INT,
    DATUM_PREUZIMANJA_STARESINSTVA DATE NOT NULL,
    NAPOMENA VARCHAR2(100),
    
    CONSTRAINT PK_RAZREDNI PRIMARY KEY(ID_NASTAVNIK),
    CONSTRAINT FK_RAZREDNI FOREIGN KEY(ID_NASTAVNIK) REFERENCES NASTAVNIK(ID),
    CONSTRAINT FK_ODELJENJE FOREIGN KEY(ID_ODELJENJE) REFERENCES ODELJENJE(ID)
);

CREATE TABLE STRUCNI_SARADNIK(
    ID_NASTAVNIK INT NOT NULL,
    BROJ_SPROVEDENIH_RAZGOVORA INT,
    STRUCNA_OBLAST VARCHAR2(50) NOT NULL,
    LICENCA VARCHAR2(50) NOT NULL,
    BROJ_ODRZANIH_RADIONICA INT,
    
    CONSTRAINT PK_STRUCNI_SARADNIK PRIMARY KEY(ID_NASTAVNIK),
    CONSTRAINT FK_STRUCNI_SARADNIK FOREIGN KEY(ID_NASTAVNIK) REFERENCES NASTAVNIK(ID)
);



CREATE TABLE PREDAJE(
    ID INT GENERATED ALWAYS AS IDENTITY,
    ID_PREDMET INT NOT NULL,
    ID_NASTAVNIK INT NOT NULL,
    
    CONSTRAINT PK_PREDAJE PRIMARY KEY(ID),
    CONSTRAINT FK_PREDAJE_PREDMET FOREIGN KEY(ID_PREDMET) REFERENCES PREDMET(ID),
    CONSTRAINT FK_PREDAJE_NASTAVNIK FOREIGN KEY(ID_NASTAVNIK) REFERENCES NASTAVNIK(ID_OSOBA)
);
